# 第十步：数据库初始化指南

## 📋 需要创建的集合（共 15 个）

1. `users` - 用户表
2. `videos` - 视频表
3. `orders` - 订单表
4. `works` - 作品表
5. `comments` - 评论表
6. `likes` - 点赞表
7. `collections` - 收藏表
8. `follows` - 关注表
9. `point_records` - 积分记录表
10. `earnings` - 收益表
11. `withdrawals` - 提现表
12. `messages` - 消息表
13. `admin_users` - 管理员表
14. `admin_logs` - 管理员日志表
15. `system_config` - 系统配置表

## 🚀 快速操作步骤

### 1. 访问云开发控制台

**地址**：https://console.cloud.tencent.com/tcb

**选择环境**：`yang0313`（环境ID: `yang0313-7g4dqwd46c63d876`）

### 2. 创建集合

1. 点击左侧菜单「**数据库**」
2. 点击「**创建集合**」
3. 输入集合名称（如 `users`）
4. **权限设置**：选择「**读取全部数据，修改本人数据**」
   - ⚠️ 例外：
     - `admin_users`、`system_config`：选择「**读取全部数据，不可修改数据**」
     - `admin_logs`：选择「**无权限（适用场景：后台流水数据）**」
5. 点击「**确定**」
6. 重复步骤 2-5，创建所有 15 个集合

### 3. 创建默认管理员

1. 进入 `admin_users` 集合
2. 点击「**添加记录**」
3. 添加字段：

```json
{
  "_id": "admin001",
  "username": "admin",
  "password": "$2a$10$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  "nickname": "超级管理员",
  "role": "super_admin",
  "createdAt": "2025-01-01T00:00:00.000Z",
  "updatedAt": "2025-01-01T00:00:00.000Z"
}
```

> **注意**：`password` 字段需要使用 bcrypt 加密后的密码。
> - 默认密码：`admin123`
> - 可以使用初始化脚本自动生成，或使用在线 bcrypt 工具

### 4. 创建系统配置

1. 进入 `system_config` 集合
2. 点击「**添加记录**」
3. 添加字段：

```json
{
  "_id": "config001",
  "key": "main",
  "platformName": "嘿哈",
  "platformLogo": "",
  "platformRate": 0.3,
  "minWithdrawAmount": 1000,
  "newUserPoints": 1000,
  "dailyCheckInPoints": 10,
  "enableWechatLogin": true,
  "enableAlipay": true,
  "enableWithdraw": true,
  "createdAt": "2025-01-01T00:00:00.000Z",
  "updatedAt": "2025-01-01T00:00:00.000Z"
}
```

## 📝 详细文档

参考：[DATABASE_INIT.md](./docs/DATABASE_INIT.md)

## ✅ 验证

完成创建后，检查：
- [ ] 15 个集合都已创建
- [ ] `admin_users` 集合有默认管理员记录
- [ ] `system_config` 集合有系统配置记录

---

**下一步**：配置 COS 存储（跨域规则）

